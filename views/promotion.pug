extends layout

block content
    script(type="module" src='/javascripts/viewmodel/promotionViewModel.js')   

    body.dx-viewport
        #container.main-container
            h4#labelTitle
            #information
            br 
            br
            #form
            #dataGrid
            #popup
    footer#sticky-footer.footer
        #info-panel